/** Site-wide notice shown above everything on every page. */
export function DemoBanner() {
  return (
    <div className="bg-orange py-1.5 text-center text-sm font-semibold uppercase tracking-wide text-white">
      DEMO SAYFASIDIR
    </div>
  );
}
