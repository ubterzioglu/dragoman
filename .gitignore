.omc/
.DS_Store
*.log
node_modules/

# Build çıktısı
dist/
*.tsbuildinfo

# Secrets — ASLA commit edilmez
.env
.env.local
.env.*.local

# Supabase CLI local link state
supabase/.temp/

# Content/scrape working files — kept local, never committed
# (mirrored WP pages, .download/.css/.js, ChatGPT images, .docx/.pdf sources,
#  handoff notes, scrape scripts). Not referenced by build or runtime.
docs/

# Python build artifacts
__pycache__/
*.pyc
